@model leave_management.Models.CreateLeaveAllocationVMClass

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.ListLeaveTypeVMClass) {
        <tr>
            <td>
                @item.Name
            </td>

            <td>
                <a class="btn btn-outline-success" asp-action="SetLeave" asp-route-id="item.LeaveTypeID">
                    <i class="fas fa-user-edit"></i> Allocate to Employee
                </a>
                @*@Html.ActionLink("Allocate to Employee", "SetLEave", new { id = item.LeaveTypeID })*@
            </td>

        </tr>
}
    </tbody>
</table>
